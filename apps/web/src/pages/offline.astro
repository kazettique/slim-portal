---
import { AppConstant } from "../constant";
import Base from "../layout/Base.astro";
---

<Base
  description={AppConstant.PAGE_META_OFFLINE.description}
  icon={AppConstant.PAGE_META_OFFLINE.icon}
  title={AppConstant.PAGE_META_OFFLINE.title}
>
  <h1>{AppConstant.PAGE_META_OFFLINE.icon} {AppConstant.PAGE_META_OFFLINE.title}</h1>
  <p>This page isn't saved for offline use. Connect to the internet and try again.</p>
  <p><a href="/">← Back to home</a></p>
</Base>
