---
import Base from "../layouts/Base.astro";
import { AppConstant } from "../constant";
---

<Base title={AppConstant.HOME_PAGE_META.title} description={AppConstant.HOME_PAGE_META.description}>
  <h1>{AppConstant.APP_TITLE}</h1>
  <p>{AppConstant.APP_DESCRIPTION}</p>
  <ul>
    <li>
      <a href={AppConstant.NEWS_PAGE_META.url}>{AppConstant.NEWS_PAGE_META.title}</a>
      <span> — </span>
      <span>{AppConstant.NEWS_PAGE_META.description}</span>
    </li>
  </ul>
</Base>
